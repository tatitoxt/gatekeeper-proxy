version: '3.8'

services:
  gatekeeper-proxy:
    build: .
    ports:
      - "8000:8000"
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - LOG_LEVEL=info
    volumes:
      - ./gatekeeper.config.json:/app/gatekeeper.config.json
    restart: unless-stopped
